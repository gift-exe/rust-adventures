use std::io;

fn main() {
    // println!("Hello, world!");

    // let mut spaces = "   ";

	// println!("{spaces}");

	// let spaces = spaces.len();

	// println!("{spaces}");

	// let spaces: String = spaces.to_string();

	// let spaces = "abc";
    // println!("{spaces}");

    // let a = [1, 2, 3, 4, 5];

    // println!("Please enter array index");
    
    // let mut index = String::new();

    // io::stdin().read_line(&mut index).expect("Failed to read line");

    // let index: usize = index.trim().parse().expect("Index entered not a number");

    // let element = a[index];

    // println!("The value of the elemernt at index {index} is : {element}");

    let c = true;

    let x = if c {5} else {6};

    println!("{x}");

}
